///
/// HybridViewWithChildrenSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ColorScheme` to properly resolve imports.
namespace margelo::nitro::test { enum class ColorScheme; }

#include "ColorScheme.hpp"
#include <functional>

namespace margelo::nitro::test {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ViewWithChildren`
   * Inherit this class to create instances of `HybridViewWithChildrenSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridViewWithChildren: public HybridViewWithChildrenSpec {
   * public:
   *   HybridViewWithChildren(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridViewWithChildrenSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridViewWithChildrenSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridViewWithChildrenSpec() override = default;

    public:
      // Properties
      virtual ColorScheme getColorScheme() = 0;
      virtual void setColorScheme(ColorScheme colorScheme) = 0;
      virtual std::function<void()> getSomeCallback() = 0;
      virtual void setSomeCallback(const std::function<void()>& someCallback) = 0;

    public:
      // Methods
      virtual void someMethod() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ViewWithChildren";
  };

} // namespace margelo::nitro::test
