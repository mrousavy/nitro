///
/// OnRampType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::test {

  /**
   * An enum which can be represented as a JavaScript enum (OnRampType).
   */
  enum class OnRampType {
    SLIGHTLEFT      SWIFT_NAME(slightleft) = 0,
    SLIGHTRIGHT      SWIFT_NAME(slightright) = 1,
    NORMALLEFT      SWIFT_NAME(normalleft) = 2,
    NORMALRIGHT      SWIFT_NAME(normalright) = 3,
    SHARPLEFT      SWIFT_NAME(sharpleft) = 4,
    SHARPRIGHT      SWIFT_NAME(sharpright) = 5,
    UTURNLEFT      SWIFT_NAME(uturnleft) = 6,
    UTURNRIGHT      SWIFT_NAME(uturnright) = 7,
  } CLOSED_ENUM;

} // namespace margelo::nitro::test

namespace margelo::nitro {

  // C++ OnRampType <> JS OnRampType (enum)
  template <>
  struct JSIConverter<margelo::nitro::test::OnRampType> final {
    static inline margelo::nitro::test::OnRampType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      int enumValue = JSIConverter<int>::fromJSI(runtime, arg);
      return static_cast<margelo::nitro::test::OnRampType>(enumValue);
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::test::OnRampType arg) {
      int enumValue = static_cast<int>(arg);
      return JSIConverter<int>::toJSI(runtime, enumValue);
    }
    static inline bool canConvert(jsi::Runtime&, const jsi::Value& value) {
      if (!value.isNumber()) {
        return false;
      }
      double number = value.getNumber();
      int integer = static_cast<int>(number);
      if (number != integer) {
        // The integer is not the same value as the double - we truncated floating points.
        // Enums are all integers, so the input floating point number is obviously invalid.
        return false;
      }
      // Check if we are within the bounds of the enum.
      return integer >= 0 && integer <= 7;
    }
  };

} // namespace margelo::nitro
