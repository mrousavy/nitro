///
/// Tone.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::test {

  /**
   * An enum which can be represented as a JavaScript union (Tone).
   */
  enum class Tone {
    COOL      SWIFT_NAME(cool) = 0,
    WARM      SWIFT_NAME(warm) = 1,
    NEUTRAL      SWIFT_NAME(neutral) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::test

namespace margelo::nitro {

  // C++ Tone <> JS Tone (union)
  template <>
  struct JSIConverter<margelo::nitro::test::Tone> final {
    static inline margelo::nitro::test::Tone fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("cool"): return margelo::nitro::test::Tone::COOL;
        case hashString("warm"): return margelo::nitro::test::Tone::WARM;
        case hashString("neutral"): return margelo::nitro::test::Tone::NEUTRAL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Tone - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::test::Tone arg) {
      switch (arg) {
        case margelo::nitro::test::Tone::COOL: return JSIConverter<std::string>::toJSI(runtime, "cool");
        case margelo::nitro::test::Tone::WARM: return JSIConverter<std::string>::toJSI(runtime, "warm");
        case margelo::nitro::test::Tone::NEUTRAL: return JSIConverter<std::string>::toJSI(runtime, "neutral");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Tone to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("cool"):
        case hashString("warm"):
        case hashString("neutral"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
