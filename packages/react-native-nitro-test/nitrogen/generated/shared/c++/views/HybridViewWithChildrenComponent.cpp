///
/// HybridViewWithChildrenComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridViewWithChildrenComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::test::views {

  extern const char HybridViewWithChildrenComponentName[] = "ViewWithChildren";

  HybridViewWithChildrenProps::HybridViewWithChildrenProps(const react::PropsParserContext& context,
                                                           const HybridViewWithChildrenProps& sourceProps,
                                                           const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    colorScheme([&]() -> CachedProp<ColorScheme> {
      try {
        const react::RawValue* rawValue = rawProps.at("colorScheme", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.colorScheme;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<ColorScheme>::fromRawValue(*runtime, value, sourceProps.colorScheme);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ViewWithChildren.colorScheme: ") + exc.what());
      }
    }()),
    someCallback([&]() -> CachedProp<std::function<void()>> {
      try {
        const react::RawValue* rawValue = rawProps.at("someCallback", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.someCallback;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void()>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.someCallback);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ViewWithChildren.someCallback: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridViewWithChildrenSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridViewWithChildrenSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("ViewWithChildren.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridViewWithChildrenProps::HybridViewWithChildrenProps(const HybridViewWithChildrenProps& other):
    react::ViewProps(),
    colorScheme(other.colorScheme),
    someCallback(other.someCallback),
    hybridRef(other.hybridRef) { }

  bool HybridViewWithChildrenProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("colorScheme"): return true;
      case hashString("someCallback"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridViewWithChildrenComponentDescriptor::HybridViewWithChildrenComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridViewWithChildrenComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                            const std::shared_ptr<const react::Props>& props,
                                                                                            react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridViewWithChildrenShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridViewWithChildrenComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridViewWithChildrenShadowNode&>(shadowNode);
    const HybridViewWithChildrenProps& props = concreteShadowNode.getConcreteProps();
    HybridViewWithChildrenState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::test::views
