///
/// HybridMeasuredViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridMeasuredViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::test::views {

  extern const char HybridMeasuredViewComponentName[] = "MeasuredView";

  HybridMeasuredViewProps::HybridMeasuredViewProps(const react::PropsParserContext& context,
                                                   const HybridMeasuredViewProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    text([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("text", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.text;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.text);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MeasuredView.text: ") + exc.what());
      }
    }()),
    fontSize([&]() -> CachedProp<double> {
      try {
        const react::RawValue* rawValue = rawProps.at("fontSize", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.fontSize;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<double>::fromRawValue(*runtime, value, sourceProps.fontSize);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MeasuredView.fontSize: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMeasuredViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMeasuredViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MeasuredView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridMeasuredViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("text"): return true;
      case hashString("fontSize"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridMeasuredViewComponentDescriptor::HybridMeasuredViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridMeasuredViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
                                                                                        react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridMeasuredViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridMeasuredViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridMeasuredViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridMeasuredViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridMeasuredViewProps>& props = std::const_pointer_cast<HybridMeasuredViewProps>(constProps);
    HybridMeasuredViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::test::views
