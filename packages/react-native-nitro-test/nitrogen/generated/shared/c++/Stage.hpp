///
/// Stage.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::test {

  /**
   * An enum which can be represented as a JavaScript union (Stage).
   */
  enum class Stage {
    IDLE      SWIFT_NAME(idle) = 0,
    WARMUP      SWIFT_NAME(warmup) = 1,
    ACTIVE      SWIFT_NAME(active) = 2,
    COOL      SWIFT_NAME(cool) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::test

namespace margelo::nitro {

  // C++ Stage <> JS Stage (union)
  template <>
  struct JSIConverter<margelo::nitro::test::Stage> final {
    static inline margelo::nitro::test::Stage fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"): return margelo::nitro::test::Stage::IDLE;
        case hashString("warmup"): return margelo::nitro::test::Stage::WARMUP;
        case hashString("active"): return margelo::nitro::test::Stage::ACTIVE;
        case hashString("cool"): return margelo::nitro::test::Stage::COOL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Stage - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::test::Stage arg) {
      switch (arg) {
        case margelo::nitro::test::Stage::IDLE: return JSIConverter<std::string>::toJSI(runtime, "idle");
        case margelo::nitro::test::Stage::WARMUP: return JSIConverter<std::string>::toJSI(runtime, "warmup");
        case margelo::nitro::test::Stage::ACTIVE: return JSIConverter<std::string>::toJSI(runtime, "active");
        case margelo::nitro::test::Stage::COOL: return JSIConverter<std::string>::toJSI(runtime, "cool");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Stage to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("idle"):
        case hashString("warmup"):
        case hashString("active"):
        case hashString("cool"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
