///
/// HybridMeasuredViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridMeasuredViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridMeasuredViewSpecSwift.hpp"
#import "NitroTest-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::test;
using namespace margelo::nitro::test::views;

/**
 * Represents the React Native View holder for the Nitro "MeasuredView" HybridView.
 */
@interface HybridMeasuredViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridMeasuredViewComponent {
  std::shared_ptr<HybridMeasuredViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridMeasuredViewComponent class]];

  // Install self-measurement iff "MeasuredView" conforms to `MeasurableView`.
  if (NitroTest::HybridMeasuredViewMeasurer::isMeasurable()) {
    HybridMeasuredViewShadowNode::measureFunction.store(
      +[](const react::ViewProps& propsBase,
          const react::LayoutContext& layoutContext,
          const react::LayoutConstraints& layoutConstraints) -> react::Size {
        const auto& props = static_cast<const HybridMeasuredViewProps&>(propsBase);
        margelo::nitro::test::MeasuredViewProps measureProps {
          /* text */ props.text.value,
          /* fontSize */ props.fontSize.value
        };
        margelo::nitro::test::LayoutContext measureContext {
          /* pointScaleFactor */ layoutContext.pointScaleFactor,
          /* isRTL */ layoutConstraints.layoutDirection == react::LayoutDirection::RightToLeft
        };
        margelo::nitro::test::LayoutConstraints measureConstraints {
          /* minimumSize */ margelo::nitro::test::Size { layoutConstraints.minimumSize.width, layoutConstraints.minimumSize.height },
          /* maximumSize */ margelo::nitro::test::Size { layoutConstraints.maximumSize.width, layoutConstraints.maximumSize.height }
        };
        auto size = NitroTest::HybridMeasuredViewMeasurer::measure(measureProps, measureContext, measureConstraints);
        return react::Size { static_cast<react::Float>(size.width), static_cast<react::Float>(size.height) };
      },
      std::memory_order_release);
  }
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridMeasuredViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridMeasuredViewSpec> hybridView = NitroTest::NitroTestAutolinking::createMeasuredView();
    _hybridView = std::dynamic_pointer_cast<HybridMeasuredViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroTest::HybridMeasuredViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridMeasuredViewProps const>(props);
  auto& newViewProps = const_cast<HybridMeasuredViewProps&>(newViewPropsConst);
  NitroTest::HybridMeasuredViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // text: string
  if (newViewProps.text.isDirty) {
    swiftPart.setText(newViewProps.text.value);
    newViewProps.text.isDirty = false;
  }
  // fontSize: number
  if (newViewProps.fontSize.isDirty) {
    swiftPart.setFontSize(newViewProps.fontSize.value);
    newViewProps.fontSize.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroTest::NitroTestAutolinking::isMeasuredViewRecyclable();
}

- (void)prepareForRecycle {
  [super prepareForRecycle];
  NitroTest::HybridMeasuredViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  NitroTest::HybridMeasuredViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  [super invalidate];
}
#endif

@end
