///
/// HybridProjectLineSpec_cxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridProjectLineSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
open class HybridProjectLineSpec_cxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::test::bridge::swift`)
   * from `NitroTest-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.test.bridge.swift

  /**
   * Holds an instance of the `HybridProjectLineSpec` Swift protocol.
   */
  private var __implementation: any HybridProjectLineSpec

  /**
   * Holds a weak pointer to the C++ class that wraps the Swift class.
   */
  private var __cxxPart: bridge.std__weak_ptr_HybridProjectLineSpec_

  /**
   * Create a new `HybridProjectLineSpec_cxx` that wraps the given `HybridProjectLineSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: any HybridProjectLineSpec) {
    self.__implementation = implementation
    self.__cxxPart = .init()
    /* no base class */
  }

  /**
   * Get the actual `HybridProjectLineSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridProjectLineSpec() -> any HybridProjectLineSpec {
    return __implementation
  }

  /**
   * Casts this instance to a retained unsafe raw pointer.
   * This acquires one additional strong reference on the object!
   */
  public func toUnsafe() -> UnsafeMutableRawPointer {
    return Unmanaged.passRetained(self).toOpaque()
  }

  /**
   * Casts an unsafe pointer to a `HybridProjectLineSpec_cxx`.
   * The pointer has to be a retained opaque `Unmanaged<HybridProjectLineSpec_cxx>`.
   * This removes one strong reference from the object!
   */
  public class func fromUnsafe(_ pointer: UnsafeMutableRawPointer) -> HybridProjectLineSpec_cxx {
    return Unmanaged<HybridProjectLineSpec_cxx>.fromOpaque(pointer).takeRetainedValue()
  }

  /**
   * Gets (or creates) the C++ part of this Hybrid Object.
   * The C++ part is a `std::shared_ptr<HybridProjectLineSpec>`.
   */
  public func getCxxPart() -> bridge.std__shared_ptr_HybridProjectLineSpec_ {
    let cachedCxxPart = self.__cxxPart.lock()
    if Bool(fromCxx: cachedCxxPart) {
      return cachedCxxPart
    } else {
      let newCxxPart = bridge.create_std__shared_ptr_HybridProjectLineSpec_(self.toUnsafe())
      __cxxPart = bridge.weakify_std__shared_ptr_HybridProjectLineSpec_(newCxxPart)
      return newCxxPart
    }
  }

  

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return MemoryHelper.getSizeOf(self.__implementation) + self.__implementation.memorySize
  }

  /**
   * Call dispose() on the Swift class.
   * This _may_ be called manually from JS.
   */
  @inline(__always)
  public func dispose() {
    self.__implementation.dispose()
  }

  /**
   * Call toString() on the Swift class.
   */
  @inline(__always)
  public func toString() -> String {
    return self.__implementation.toString()
  }

  // Properties
  public final var coordinates: bridge.std__vector_std__shared_ptr_HybridLatLngSpec__ {
    @inline(__always)
    get {
      return { () -> bridge.std__vector_std__shared_ptr_HybridLatLngSpec__ in
        var __vector = bridge.create_std__vector_std__shared_ptr_HybridLatLngSpec__(self.__implementation.coordinates.count)
        for __item in self.__implementation.coordinates {
          __vector.push_back({ () -> bridge.std__shared_ptr_HybridLatLngSpec_ in
            let __cxxWrapped = __item.getCxxWrapper()
            return __cxxWrapped.getCxxPart()
          }())
        }
        return __vector
      }()
    }
    @inline(__always)
    set {
      self.__implementation.coordinates = newValue.map({ __item in { () -> HybridLatLngSpec in
        let __unsafePointer = bridge.get_std__shared_ptr_HybridLatLngSpec_(__item)
        let __instance = HybridLatLngSpec_cxx.fromUnsafe(__unsafePointer)
        return __instance.getHybridLatLngSpec()
      }() })
    }
  }

  // Methods
  
}
