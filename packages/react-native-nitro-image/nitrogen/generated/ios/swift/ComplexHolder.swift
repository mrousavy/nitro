///
/// ComplexHolder.swift
/// Wed Aug 28 2024
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `ComplexHolder`, backed by a C++ struct.
 */
public typealias ComplexHolder = margelo.nitro.image.ComplexHolder

public extension ComplexHolder {
  private typealias bridge = margelo.nitro.image.bridge.swift

  /**
   * Create a new instance of `ComplexHolder`.
   */
  init(first: Dictionary<String, String>) {
    self.init({ () -> bridge.std__unordered_map_std__string__std__string_ in
      var map = bridge.create_std__unordered_map_std__string__std__string_(first.count)
      for (k, v) in first {
        map[std.string(k)] = std.string(v)
      }
      return map
    }())
  }

  var first: Dictionary<String, String> {
    @inline(__always)
    get {
      return { () -> Dictionary<String, String> in
        var dictionary = Dictionary<String, String>(minimumCapacity: self.__first.size())
        let keys = bridge.get_std__unordered_map_std__string__std__string__keys(self.__first)
        for key in keys {
          let value = self.__first[key]!
          dictionary[String(key)] = String(value)
        }
        return dictionary
      }()
    }
    @inline(__always)
    set {
      self.__first = { () -> bridge.std__unordered_map_std__string__std__string_ in
        var map = bridge.create_std__unordered_map_std__string__std__string_(newValue.count)
        for (k, v) in newValue {
          map[std.string(k)] = std.string(v)
        }
        return map
      }()
    }
  }
}
